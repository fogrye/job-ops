#!/bin/sh

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks is required to commit. Install it from https://github.com/gitleaks/gitleaks." >&2
  exit 1
fi

exec gitleaks git --pre-commit --staged --redact --no-banner
